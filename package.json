{
  "name": "@qiwi/consul-service-discovery",
  "version": "1.3.1",
  "description": "Consul service discovery",
  "main": "target/es5/index.js",
  "scripts": {
    "lint": "tslint -p tsconfig.json",
    "lint:fix": "yarn lint --fix",
    "jest": "jest -w 2",
    "test": "yarn jest",
    "pretest": "yarn lint",
    "test_with_report": "yarn test && yarn coveralls_push",
    "coveralls_push": "cat ./coverage/lcov.info | coveralls",
    "build": "yarn clean && yarn build:es5 && yarn build:es6 && yarn build:ts",
    "build:es5": "mkdir -p target/es5 && tsc -p tsconfig.build.json --target ES5 --outDir target/es5",
    "build:es6": "mkdir -p target/es6 && tsc -p tsconfig.build.json --target ES6 --outDir target/es6",
    "build:ts": "cp -r src/main/ts target/ts",
    "clean": "rm -rf ./target"
  },
  "keywords": [
    "consul",
    "consul-client",
    "consul-service-discovery"
  ],
  "authors": [
    "Anton Golub <golub.anton@gmail.com>",
    "Mikhail Dolgikh <wearebear80@gmail.com>"
  ],
  "repository": {
    "type": "git",
    "url": "git@github.com:qiwi/consul-service-discovery.git"
  },
  "license": "ISC",
  "dependencies": {
    "consul": "^0.34.1",
    "lodash": "^4.17.11"
  },
  "devDependencies": {
    "@qiwi/substrate": "^1.9.1",
    "@semantic-release/changelog": "^3.0.2",
    "@semantic-release/git": "^7.0.8",
    "@semantic-release/github": "^5.2.10",
    "@semantic-release/npm": "^5.1.4",
    "@types/bluebird": "^3.5.26",
    "@types/consul": "^0.23.33",
    "@types/jest": "^24.0.11",
    "@types/lodash": "^4.14.123",
    "babel-core": "^6.26.3",
    "bluebird": "^3.5.4",
    "coveralls": "^3.0.3",
    "jest": "^24.7.1",
    "semantic-release": "^15.13.3",
    "ts-jest": "^24.0.1",
    "tslint": "^5.15.0",
    "tslint-config-standard": "^8.0.1",
    "typescript": "^3.4.1"
  },
  "files": [
    "README.md",
    "CHANGELOG.md",
    "target"
  ],
  "jest": {
    "collectCoverage": true,
    "collectCoverageFrom": [
      "<rootDir>/src/main/ts/*.+(ts|tsx|js)"
    ],
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js"
    ],
    "transform": {
      "^.+\\.(ts|tsx)$": "ts-jest"
    },
    "globals": {
      "ts-jest": {
        "tsConfig": "tsconfig.json"
      }
    },
    "testMatch": [
      "<rootDir>src/test/**/*.+(ts|tsx|js)"
    ]
  },
  "release": {
    "branch": "master",
    "verifyConditions": [
      "@semantic-release/changelog",
      "@semantic-release/npm",
      "@semantic-release/git"
    ],
    "prepare": [
      "@semantic-release/changelog",
      "@semantic-release/npm",
      "@semantic-release/git"
    ],
    "publish": [
      "@semantic-release/npm",
      "@semantic-release/github"
    ]
  }
}
